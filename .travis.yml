language: python
python:
    - "2.7"
addons:
  apt:
    packages:
        - gfortran 
        - libblas-dev 
        - liblapack-dev 
install:
# Install Wannier
    - cp config/make.sys.gfort.travisci make.sys
    - make -j default
script:
    - make test
git:
  depth: 3
